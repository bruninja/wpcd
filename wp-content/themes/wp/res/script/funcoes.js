//$(document).ready(function () {
//    //$("#menu a[href='" + location.href.substring(location.href.lastIndexOf("/") + 1, 255) + "']").addClass("ativo");
//    var link = document.location.href.split('/').slice(-1);   // retrieve page name
//    $('#menu a.ativo').removeClass('ativo');        // remove class 'active'
//    $('#menu a[href$="' + link + '"]').addClass('ativo'); // and add it to a matching link
//});
